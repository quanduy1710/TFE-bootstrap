---
# Inventory for GitLab self-hosted on EC2
# The playbook runs locally on the EC2 instance itself (no SSH required).

all:
  children:
    gitlab:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
